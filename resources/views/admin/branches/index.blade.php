@extends('admin.layout') @section('content')<div class="d-flex justify-content-between"><h1 class="h3">Branches</h1><a class="btn btn-dark" href="{{ route('admin.branches.create') }}">Add Branch</a></div><table class="table bg-white mt-3"><thead><tr><th>Name</th><th>Active</th><th></th></tr></thead><tbody>@foreach($branches as $branch)<tr><td>{{ $branch->name_en }}</td><td>{{ $branch->is_active ? 'Yes':'No' }}</td><td class="text-end"><a href="{{ route('admin.branches.edit',$branch) }}" class="btn btn-sm btn-outline-dark">Edit</a><form class="d-inline" method="post" action="{{ route('admin.branches.destroy',$branch) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@endforeach</tbody></table>@endsection